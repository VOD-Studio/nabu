<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfy.nabu.forum.mapper.TopicMapper">

    <resultMap id="TopicResultMap" type="com.xfy.nabu.forum.domain.TopicEntity">
        <id property="id" column="id"/>
        <result property="boardId" column="board_id"/>
        <result property="authorId" column="author_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="viewCount" column="view_count"/>
        <result property="commentCount" column="comment_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="pinned" column="pinned"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="Columns">
        id, board_id, author_id, title, content, view_count, comment_count, like_count, pinned, status, created_at, updated_at, deleted
    </sql>

    <select id="selectById" resultMap="TopicResultMap">
        select <include refid="Columns"/> from t_topic where id = #{id} and deleted = 0
    </select>

    <!-- 置顶帖 (pinned=1) 排在最前，其余按 created_at desc 排列 -->
    <select id="listByBoard" resultMap="TopicResultMap">
        select <include refid="Columns"/> from t_topic
        where board_id = #{boardId} and deleted = 0
        order by pinned desc, created_at desc
        limit #{offset}, #{limit}
    </select>

    <select id="countByBoard" resultType="long">
        select count(*) from t_topic where board_id = #{boardId} and deleted = 0
    </select>

    <insert id="insert" parameterType="com.xfy.nabu.forum.domain.TopicEntity" useGeneratedKeys="true" keyProperty="id">
        insert into t_topic (board_id, author_id, title, content, view_count, comment_count, like_count, pinned, status)
        values (#{boardId}, #{authorId}, #{title}, #{content}, #{viewCount}, #{commentCount}, #{likeCount}, #{pinned}, #{status})
    </insert>

    <update id="incrementCommentCount">
        update t_topic set comment_count = comment_count + 1 where id = #{id} and deleted = 0
    </update>

    <update id="updatePinned">
        update t_topic set pinned = #{pinned} where id = #{id} and deleted = 0
    </update>

</mapper>
